<UserControl x:Class="Restaurant.Desktop.Controls.ToastNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="350">
    <Grid>
        <Border x:Name="border"
                Background="{StaticResource BrushSurface}" 
                BorderThickness="1,1,1,4" 
                CornerRadius="8" 
                Padding="16,12" 
                Margin="8"
                MinWidth="300"
                MaxWidth="450"
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.1" Color="Black"/>
            </Border.Effect>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Icon Container -->
                <Border x:Name="iconBorder" Grid.Column="0" Width="28" Height="28" CornerRadius="14" Margin="0,0,12,0" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Path x:Name="iconPath" Stretch="Uniform" Width="14" Height="14" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Border>

                <!-- Message Text -->
                <TextBlock Grid.Column="1" x:Name="txtMessage" Text="رسالة افتراضية للتوست" Style="{StaticResource TextStyle.Label}" FontWeight="SemiBold" VerticalAlignment="Center" TextWrapping="Wrap"/>

                <!-- Close Button -->
                <Button Grid.Column="2" x:Name="btnClose" Click="BtnClose_Click" Style="{StaticResource ButtonStyle.Secondary}" Padding="4" Width="24" Height="24" BorderThickness="0" Background="Transparent" Margin="8,0,0,0" VerticalAlignment="Center" Focusable="False">
                    <Path Data="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z" Fill="{StaticResource BrushTextMuted}" Stretch="Uniform" Width="10" Height="10"/>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
