<Window x:Class="Restaurant.Desktop.Controls.ConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="تأكيد العملية" 
        Height="220" Width="400"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent"
        ShowInTaskbar="False"
        FlowDirection="RightToLeft">
    
    <Border Background="{StaticResource BrushSurface}" 
            BorderBrush="{StaticResource BrushBorder}" 
            BorderThickness="1" 
            CornerRadius="12" 
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.15" Color="Black"/>
        </Border.Effect>
        
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title & Icon -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="iconBorder" Width="28" Height="28" CornerRadius="14" Background="#15EF4444" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,10,0">
                    <Path x:Name="iconPath" Data="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z" Fill="{StaticResource BrushDanger}" Stretch="Uniform" Width="14" Height="14" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Border>
                <TextBlock Grid.Column="1" x:Name="txtTitle" Text="تأكيد الحذف" Style="{StaticResource TextStyle.H3}" FontWeight="Bold" VerticalAlignment="Center"/>
            </Grid>

            <!-- Message Description -->
            <TextBlock Grid.Row="1" x:Name="txtMessage" Text="هل أنت متأكد من رغبتك في حذف هذا العنصر؟ لا يمكن التراجع عن هذا الإجراء لاحقاً." Style="{StaticResource TextStyle.Body}" Foreground="{StaticResource BrushTextMuted}" TextWrapping="Wrap" VerticalAlignment="Top" Margin="0,0,0,16"/>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnCancel" Content="إلغاء" Click="BtnCancel_Click" Style="{StaticResource ButtonStyle.Secondary}" Height="36" Padding="16,0" Margin="0,0,8,0" IsCancel="True"/>
                <Button x:Name="btnConfirm" Content="تأكيد الحذف" Click="BtnConfirm_Click" Style="{StaticResource ButtonStyle.Danger}" Height="36" Padding="16,0" IsDefault="True"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
