<UserControl x:Class="Restaurant.Desktop.Controls.LoadingSpinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Restaurant.Desktop.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="120"
             HorizontalAlignment="Center" VerticalAlignment="Center">
    <StackPanel x:Name="SpinnerContainer" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid HorizontalAlignment="Center">
            <!-- Simple modern circle loading indicator using border rotation -->
            <Border Width="40" Height="40" 
                    BorderThickness="4" 
                    CornerRadius="20" 
                    BorderBrush="{StaticResource BrushBorder}">
            </Border>
            
            <Border Width="40" Height="40" 
                    BorderThickness="4,0,0,0" 
                    CornerRadius="20" 
                    BorderBrush="{StaticResource BrushPrimary}"
                    RenderTransformOrigin="0.5,0.5">
                <Border.RenderTransform>
                    <RotateTransform x:Name="SpinnerRotate" Angle="0"/>
                </Border.RenderTransform>
                <Border.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="SpinnerRotate" 
                                                 Storyboard.TargetProperty="Angle" 
                                                 From="0" To="360" 
                                                 Duration="0:0:1" 
                                                 RepeatBehavior="Forever"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Border.Triggers>
            </Border>
        </Grid>
        
        <!-- Loading Text -->
        <TextBlock x:Name="TxtLoading" 
                   Text="جاري التحميل..." 
                   FontFamily="{StaticResource CairoFont}" 
                   FontSize="12" 
                   FontWeight="SemiBold"
                   Foreground="{StaticResource BrushPrimary}" 
                   Margin="0,8,0,0" 
                   HorizontalAlignment="Center"/>
    </StackPanel>
    
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <!-- Pulse opacity effect on loading text -->
                    <DoubleAnimation Storyboard.TargetName="TxtLoading"
                                     Storyboard.TargetProperty="Opacity"
                                     From="0.5" To="1.0"
                                     Duration="0:0:0.8"
                                     AutoReverse="True"
                                     RepeatBehavior="Forever"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>
</UserControl>
